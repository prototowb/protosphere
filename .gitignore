# Dependencies
node_modules/
pnpm-lock.yaml
yarn.lock
package-lock.json

# Build output (gitignored locally; CI force-adds with -f on every push)
dist/
.output/
.nuxt/

# Environment variables (local development + staging secrets)
.env
.env.local
.env.*.local
.env.staging

# IDE & Editor
.idea/
# Track project-level VS Code config (settings.json, extensions.json)
# but ignore personal/machine-specific overrides
.vscode/*.local.json
.vscode/launch.json
*.swp
*.swo
*~
.DS_Store
*.sublime-workspace
*.sublime-project

# OS-specific
Thumbs.db
.DS_Store

# Supabase local development
supabase/.branches/
supabase/.temp/
.supabase/

# Logs
logs/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Testing
coverage/
.nyc_output/

# Temporary files
.cache/
tmp/
temp/

# Development
.turbo/

# Twemoji SVG assets committed to repo; run `npm run sync:emoji` to update
