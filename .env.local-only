# localStorage-only mode — no Supabase or Docker required.
# Used by: npm run dev:local
# Overrides .env.local so the app runs purely in-browser with localStorage.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
