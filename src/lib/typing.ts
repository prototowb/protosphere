/** How long a typing indicator stays visible before expiring (ms). */
export const TYPING_EXPIRE_MS = 2000

/** How long after the last keystroke before the typing indicator is cleared (ms). */
export const STOP_AFTER_MS = 1500
