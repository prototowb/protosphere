name: "Monitoring Setup Workflow"
type: "workflow"
version: "1.0.0"
description: "Setting up monitoring, logging, and alerting for production systems"

category: "devops"
tags: ["monitoring", "logging", "alerting", "observability", "devops"]

status: "stable"
author: "Proto Gear Team"
last_updated: "2025-12-09"

dependencies:
  required: []
  optional:
    - "workflows/cicd-setup"
  suggested:
    - "skills/performance"

conflicts: []

composable_with:
  - "skills/performance"
  - "workflows/cicd-setup"

agent_roles:
  - "DevOps Agent"
  - "Monitoring Specialist"
  - "Infrastructure Agent"
  - "SRE Agent"

relevance:
  triggers:
    - "monitoring"
    - "logging"
    - "alerting"
    - "observability"
    - "metrics"
  contexts:
    - "Before production deployment"
    - "For system reliability"
    - "After infrastructure changes"

usage_notes: |
  Monitoring and observability setup workflow.

  Includes: metrics collection, log aggregation, alerting rules, dashboards.
  Essential for production systems and incident response.

required_files: []

optional_files:
  - "ARCHITECTURE.md"

workflow:
  steps: 8
  estimated_duration: "3-5 hours"
  outputs:
    - "type: configuration"
    - "type: monitoring"
